{
  "kind": "collectionType",
  "collectionName": "events",
  "info": {
    "name": "Event"
  },
  "options": {
    "increments": true,
    "timestamps": true
  },
  "attributes": {
    "action": {
      "type": "enumeration",
      "enum": [
        "create",
        "update",
        "delete"
      ]
    },
    "item": {
      "type": "string"
    },
    "name": {
      "type": "string"
    },
    "user": {
      "via": "events",
      "plugin": "users-permissions",
      "model": "user"
    }
  }
}
